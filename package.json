{
  "name": "angular-unit-test-generator",
  "version": "1.0.0",
  "description": "generating angular unit test",
  "keywords": [
    "angular",
    "unit-test",
    "generator",
    "jest",
    "jasmine",
    "unit",
    "testing",
    "aug"
  ],
  "license": "UNLICENSED",
  "main": "index.js",
  "scripts": {
    "start": "ts-node examples/index.ts",
    "build": "tsc && yarn copy:files",
    "copy:files": "yarn copy:assets && cpx package.json dist && cpx 'src/aug.json' dist",
    "copy:assets": "cpx 'src/templates/**' dist/templates && cpx 'testing-utils/**' dist/testing-utils",
    "test": "jest --config jestconfig.json"
  },
  "author": "Vitalii Kotliar",
  "devDependencies": {
    "@types/jest": "^25.1.3",
    "@types/node": "^13.7.1",
    "cpx": "^1.5.0",
    "jest": "^25.1.0",
    "ts-jest": "^25.2.1",
    "ts-node": "^8.6.2",
    "typescript": "^3.7.5"
  },
  "bin": {
    "aug": "./cli.js"
  },
  "dependencies": {
    "find-config": "^1.0.0"
  }
}
